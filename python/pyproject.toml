[project]
name = "mgt159t-youtube-rdd"
version = "0.0.0"
description = "MGT 159T final project — YouTube mid-roll ad threshold RDD"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "marimo>=0.10",
    "pandas>=2.2",
    "altair>=5.4",
    "pyarrow>=17.0",
]
